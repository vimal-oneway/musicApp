import { Layout } from '../../components/Layout/Layout'
import { SearchContainer } from '../../components/Search/Search'

export const Search = () => {
  return (
    <Layout>
      <SearchContainer />
    </Layout>
  )
}
